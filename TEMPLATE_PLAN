for each example:
	One line summary
	List of how-tos covered
	Expected accuracy/metric etc
	Required Memory
	Expected runtime - raw for some config, and as a ratio of one of the simpler ex
	Parameters to run (heap, off heap etc)/Run command
	Other related examples
	Next steps/what to look at next

Other:
Standardize maintainence todo (this is a work around for a bug in current release that will be fixed etc)
Hidden run script file that is like a test - what does it mean to say this example works? Runs without errors, what else? expected value of accuracy/training metric? clock run time as a ratio of shortest running test for regression testing
How to handle deprecated APIs? Script to check
